// Decoder provenance is a domain value so domain segments never depend on an
// engine implementation. This engine-facing export keeps the R11 API grouped
// with the chord assembler.
export '../../domain/analysis_segment.dart' show DecoderSource;
